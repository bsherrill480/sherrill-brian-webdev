mixin listItem(name)
    div.list-item
        a(href="#") #{name}

mixin editableListItem(name, itemHref, editHref)
    div.list-item
        a(href="#{itemHref}") #{name}
        div.pull-right
            a(href="#{editHref}")
                +cogButton
